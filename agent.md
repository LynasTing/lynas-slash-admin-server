# AGENT.md

## 使用方式

开始分析、修改或生成代码前，必须先阅读并遵循：

- `ai/project-context.md`
- `ai/rules/java-code-style.md`
- `ai/rules/spring-application.md`
- `ai/rules/api-and-data.md`
- `ai/rules/testing.md`
- `ai/rules/git-commit-message.md`
- `ai/workflow/development-workflow.md`
- `ai/workflow/pr-review-flow.md`

## 执行要求

- 以 `ai/rules` 中更具体的规则覆盖通用规则；与现有项目约束冲突时，以项目实际配置和用户明确要求为准。
- 改动前确认受影响的模块、接口、数据库对象和测试边界；不要仅凭文件名猜测业务意图。
- 改动后执行与风险匹配的验证，并在交付说明中列出已遵循的规则和实际执行的验证命令。
- 生成提交信息前，先读取暂存区的真实改动，并遵循 `ai/rules/git-commit-message.md`。

## Java 学习阶段协作约定

- 开发者目前处于 Java 学习阶段。生成或修改代码时，优先采用 Java 官方文档、Spring 官方文档和主流社区广泛接受的实践；不得为了炫技引入冷门技巧、过度抽象或不必要的框架能力。
- 优先写清晰、直白、便于调试的实现。使用语言特性、设计模式或框架能力前，先确认它能解决当前问题；若有更简单且同样可靠的写法，优先选择简单写法。
- 交付代码时，除说明“改了什么”外，还要简要说明关键代码“为什么这样写”：涉及的类职责、调用链、重要变量、方法参数与返回值、异常处理和数据流。不要把推测当作事实；无法从项目中确认的信息必须明确说明。
- 对初学者不直观的部分补充注释或说明，包括但不限于：类的职责、字段的业务含义、方法的输入输出与边界、泛型、集合操作、异常处理、事务、校验、序列化，以及注解在当前位置的实际作用和生效范围。
- 注释必须准确、紧贴代码并解释意图、约束或原因；不得用注释机械复述显而易见的赋值、getter/setter 或语法。字段注释、Javadoc 和单行注释的格式仍以 `ai/rules/java-code-style.md` 为准。
- 新增代码应尽量使用明确的类型、命名和控制流，避免把多层逻辑压缩成难以阅读的链式调用。每个公共方法和非显然的私有方法都应有足以让学习者理解的说明。
- 遇到多个合理实现方案时，说明推荐方案及其取舍；涉及 API、数据库、并发、安全、事务或兼容性时，必须指出风险和验证方式，不能只给出能编译的代码。
- 修复错误时，先定位并解释根因、影响范围和触发条件，再给出最小且符合项目规范的改动；同时补充或建议覆盖该问题的测试。
- 除非用户明确要求只要结论或只要代码，回答应使用中文；代码标识符、标准 API 名称和必要的技术术语保留原文。
